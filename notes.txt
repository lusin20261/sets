sudo apt purge -y 'libreoffice*'
rm -rf ~/.config/libreoffice
sudo apt purge -y blueman bluez bluez-tools pulseaudio-module-bluetooth
sudo apt purge -y 'bluez*'
sudo apt autoremove --purge -y
sudo apt clean
xdg-settings set default-web-browser brave-browser.desktop
sudo usermod -aG video,render lab
sudo apt install -y flatpak
sudo flatpak remote-add --if-not-exists flathub https://dl.flathub.org/repo/flathub.flatpakrepo
sudo flatpak install -y flathub edu.mit.Scratch
sudo apt remove --purge -y cmatrix
sudo apt install -y cmatrix
rm -rf ~/Plantillas/tesoro+20_puntos.xd
touch ~/Plantillas/tesoro+20_puntos.xd
rm ~/.local/share/applications/cerrar-programas.desktop
mkdir ~/Cosas
mkdir ~/Alumnos
mkdir ~/Puntos
mkdir ~/Aqui
mkdir ~/Papelera
